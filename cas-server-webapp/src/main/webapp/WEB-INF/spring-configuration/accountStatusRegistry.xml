<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       	  http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

	<bean class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor" />
	
	<bean id="persistenceUnitManager" class="org.springframework.orm.jpa.persistenceunit.DefaultPersistenceUnitManager"
		p:persistenceXmlLocation="classpath:/edu/asu/cas/web/support/edna/persistence.xml" />
		
	<bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceUnitManager" ref="persistenceUnitManager" />
		<property name="persistenceUnitName" value="edna" />
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
				<property name="showSql" value="false" />
			</bean>
		</property>
	</bean>
	
	<bean id="accountStatusRegistry" class="edu.asu.cas.web.support.edna.EDNAAccountStatusRegistry"
		p:workerCount="${cas.accountStatus.workerCount}"
		p:workerTimeoutMillis="${cas.accountStatus.workerTimeoutMillis}"
		p:alertSender="${cas.accountStatus.alertSender}"
		p:alertSubject="${cas.accountStatus.alertSubject}"
		p:alertRecipients="${cas.accountStatus.alertRecipients}" />

</beans>
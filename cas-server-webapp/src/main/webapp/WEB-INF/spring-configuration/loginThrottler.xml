<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="loginThrottler" class="edu.asu.cas.web.flow.throttle.LoginThrottler"
          p:failureThreshold="${cas.throttling.failureThreshold}"
          p:failureRangeSeconds="${cas.throttling.failureRangeSeconds}"
          p:lockoutPeriodSeconds="${cas.throttling.lockoutPeriodSeconds}" />
          
    <bean id="loginThrottlerCleanerJobDetail"
          class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean"
          p:targetObject-ref="loginThrottler"
          p:targetMethod="vacuum" />

    <bean id="periodicLoginThrottlerCleanerTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerBean"
          p:jobDetail-ref="loginThrottlerCleanerJobDetail"
          p:startDelay="${cas.throttling.cleanerIntervalMillis}"
          p:repeatInterval="${cas.throttling.cleanerIntervalMillis}" />

</beans>